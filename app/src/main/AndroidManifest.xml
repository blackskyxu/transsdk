<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.transsnet.transsdktest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".base.BaseApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name="com.transsnet.transsdktest.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".VideoListActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="com.transsnet.vsvideosdkdemo.VsGlideModule"
            android:value="VsGlideModule" />
        <activity android:name=".player.PlayerActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>